package model;

/**
 * Use parser to interpret the input 
 * and run the instruction, giving back a final state of all the GUI elements 
 * to update the view
 *
 */
public class Model {

	/**
	 * Simulation of all the elements in GUI. 
	 * @param parser the parser which interpretate the input string into instructions
	 * @param view: GUI elements
	 */
	public Model(Parser parser, View view){
		
	}
}
